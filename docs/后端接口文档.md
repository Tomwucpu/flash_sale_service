# 高并发秒杀兑换平台后端接口文档

> 文档版本：v1.8  
> 编写日期：2026-04-21  
> 适用范围：**仅包含当前代码中已经开发完成并可调用的业务接口**  
> 当前阶段：已完成 `Task 1`、`Task 2`、`Task 3`、`Task 4`、`Task 5`、`Task 6`、`Task 7`、`Task 8`，并补充了 `Task 9` 联调所需的订单详情查询与自动化脚本

## 1. 文档范围

当前仓库里已经落地的业务接口来自以下五个服务：

- `flash-sale-user-service`：用户注册、登录、当前用户查询、后台按 ID 查询用户
- `flash-sale-activity-service`：活动创建、编辑、详情、列表、按配置发布、提前发布、下线、删除、第三方兑换码导入、导入批次查询、导入失败明细查询、用户侧公开活动列表与详情查询
- `flash-sale-seckill-service`：秒杀资格尝试、结果查询
- `flash-sale-order-service`：订单详情查询、兑换码按订单号查询、导出任务、异常补偿台账、导出文件下载
- `flash-sale-payment-service`：模拟支付单创建、支付回调

## 2. 访问说明

### 2.1 推荐访问入口

推荐统一通过 Gateway 访问：

- Gateway 地址：`http://localhost:18080`
- 用户服务路由前缀：`/api/users`

完整示例：

- `POST http://localhost:18080/api/users/register`
- `POST http://localhost:18080/api/users/login`
- `GET http://localhost:18080/api/users/me`
- `POST http://localhost:18080/api/activities`
- `POST http://localhost:18080/api/activities/{activityId}/publish`
- `POST http://localhost:18080/api/activities/{activityId}/advance-publish`
- `POST http://localhost:18080/api/activities/{activityId}/codes/import`
- `GET http://localhost:18080/api/activities/{activityId}/codes/import-batches`
- `DELETE http://localhost:18080/api/activities/{activityId}`
- `GET http://localhost:18080/api/public/activities`
- `GET http://localhost:18080/api/public/activities/{activityId}`
- `POST http://localhost:18080/api/seckill/activities/{activityId}/attempt`
- `GET http://localhost:18080/api/seckill/results/{activityId}`
- `GET http://localhost:18080/api/codes/orders/{orderNo}`

### 2.2 鉴权规则

- `POST /api/users/register`：公开接口，无需登录
- `POST /api/users/login`：公开接口，无需登录
- `GET /api/public/activities`：公开接口，无需登录
- `GET /api/public/activities/{activityId}`：公开接口，无需登录
- `GET /api/users/me`：需携带 `Authorization: Bearer <accessToken>`
- `GET /api/users/{userId}`：需携带 `Authorization: Bearer <accessToken>`，且角色必须为 `ADMIN` 或 `PUBLISHER`
- `/api/activities/**`：需携带 `Authorization: Bearer <accessToken>`，且角色必须为 `ADMIN` 或 `PUBLISHER`
- `/api/seckill/**`：需携带 `Authorization: Bearer <accessToken>`
- `/api/codes/**`：需携带 `Authorization: Bearer <accessToken>`

### 2.3 请求头约定

建议所有请求都带上：

- `Content-Type: application/json`
- `X-Request-Id: <唯一请求号>`

特殊说明：

- `POST /api/activities/{activityId}/codes/import` 为文件上传接口，请使用 `multipart/form-data`
- 文件字段名固定为 `file`

说明：

- 当前系统**不会自动生成** `X-Request-Id`
- 如果请求带了 `X-Request-Id`，响应体中的 `requestId` 会原样回传
- 如果未传，响应体中的 `requestId` 为 `null`
- `POST /api/seckill/activities/{activityId}/attempt` 当前会强制校验 `X-Request-Id`

### 2.4 直连服务说明

如果不走 Gateway，直接访问 `flash-sale-user-service`（默认端口 `9001`），则：

- `register`、`login` 仍可直接调用
- `me`、`/{userId}` 不会自动解析 `Bearer Token`
- 这两个接口依赖上游透传的用户头：
  - `X-User-Id`
  - `X-Username`
  - `X-Role`

因此联调时建议统一走 Gateway。

## 3. 统一响应格式

所有已实现接口都返回统一结构：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-20260415-0001",
  "data": {}
}
```

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | string | 业务码，成功时固定为 `SUCCESS` |
| `message` | string | 响应描述，成功时固定为 `ok` |
| `requestId` | string/null | 请求头 `X-Request-Id` 的透传值 |
| `data` | object/null | 业务数据 |

## 4. 通用错误码

当前代码中已经明确实现的错误码如下：

| HTTP 状态码 | 业务码 | 含义 |
| --- | --- | --- |
| `400` | `INVALID_ARGUMENT` | 参数非法、用户名重复、用户名或密码错误、用户不存在、用户被禁用 |
| `401` | `UNAUTHORIZED` | 未登录、缺少 Bearer Token、Token 无效或已过期 |
| `403` | `FORBIDDEN` | 当前角色无权访问资源 |
| `500` | `SYSTEM_ERROR` | 未被显式处理的服务端异常 |

错误响应示例：

```json
{
  "code": "UNAUTHORIZED",
  "message": "缺少有效的 Bearer Token",
  "requestId": "REQ-20260415-0002",
  "data": null
}
```

## 5. 数据对象

### 5.1 UserProfileResponse

```json
{
  "id": 1,
  "username": "buyer",
  "role": "USER",
  "status": "ENABLED",
  "nickname": "普通用户",
  "phone": "13800000003"
}
```

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | number | 用户 ID |
| `username` | string | 用户名 |
| `role` | string | 用户角色，当前取值：`ADMIN`、`PUBLISHER`、`USER` |
| `status` | string | 用户状态，当前取值：`ENABLED`、`DISABLED` |
| `nickname` | string/null | 昵称 |
| `phone` | string/null | 手机号 |

### 5.2 LoginResponse

```json
{
  "accessToken": "<JWT>",
  "user": {
    "id": 1,
    "username": "buyer",
    "role": "USER",
    "status": "ENABLED",
    "nickname": "普通用户",
    "phone": "13800000003"
  }
}
```

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `accessToken` | string | JWT 访问令牌，默认有效期 `2h` |
| `user` | object | 当前登录用户信息，结构同 `UserProfileResponse` |

### 5.3 ActivitySummaryResponse

```json
{
  "id": 1001,
  "title": "5元礼品卡秒杀",
  "totalStock": 100,
  "availableStock": 100,
  "publishMode": "IMMEDIATE",
  "publishStatus": "PUBLISHED",
  "phase": "PREVIEW",
  "publishTime": "2026-04-16T09:00:00",
  "startTime": "2026-04-16T10:00:00",
  "endTime": "2026-04-16T10:30:00"
}
```

### 5.4 ActivityDetailResponse

```json
{
  "id": 1001,
  "title": "5元礼品卡秒杀",
  "description": "限量兑换",
  "coverUrl": "https://example.com/activity.png",
  "totalStock": 100,
  "availableStock": 100,
  "priceAmount": 0,
  "needPayment": false,
  "purchaseLimitType": "SINGLE",
  "purchaseLimitCount": 1,
  "codeSourceMode": "SYSTEM_GENERATED",
  "publishMode": "IMMEDIATE",
  "publishStatus": "PUBLISHED",
  "phase": "PREVIEW",
  "publishTime": "2026-04-16T09:00:00",
  "startTime": "2026-04-16T10:00:00",
  "endTime": "2026-04-16T10:30:00"
}
```

### 5.5 RedeemCodeImportBatchSummaryResponse

```json
{
  "batchNo": "IMP-1001-20260418153000001-1234",
  "fileName": "codes.csv",
  "totalCount": 100,
  "successCount": 96,
  "failedCount": 4
}
```

### 5.6 RedeemCodeImportFailureResponse

```json
{
  "lineNumber": 4,
  "rawCode": "BAD CODE",
  "reason": "INVALID_FORMAT"
}
```

### 5.7 RedeemCodeImportBatchDetailResponse

```json
{
  "batchNo": "IMP-1001-20260418153000001-1234",
  "fileName": "codes.csv",
  "totalCount": 5,
  "successCount": 2,
  "failedCount": 3,
  "failures": [
    {
      "lineNumber": 3,
      "rawCode": "CODE1001",
      "reason": "DUPLICATE_IN_FILE"
    },
    {
      "lineNumber": 4,
      "rawCode": "BAD CODE",
      "reason": "INVALID_FORMAT"
    },
    {
      "lineNumber": 5,
      "rawCode": "",
      "reason": "EMPTY_CODE"
    }
  ]
}
```

## 6. 已实现接口清单

| 序号 | 方法 | 路径 | 是否登录 | 角色要求 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 1 | `POST` | `/api/users/register` | 否 | 无 | 用户注册 |
| 2 | `POST` | `/api/users/login` | 否 | 无 | 用户登录 |
| 3 | `GET` | `/api/users/me` | 是 | 任意已登录用户 | 获取当前登录用户 |
| 4 | `GET` | `/api/users/{userId}` | 是 | `ADMIN`/`PUBLISHER` | 后台按 ID 查询用户 |
| 5 | `POST` | `/api/activities` | 是 | `ADMIN`/`PUBLISHER` | 创建活动 |
| 6 | `PUT` | `/api/activities/{activityId}` | 是 | `ADMIN`/`PUBLISHER` | 编辑未发布活动 |
| 7 | `GET` | `/api/activities/{activityId}` | 是 | `ADMIN`/`PUBLISHER` | 查询活动详情 |
| 8 | `GET` | `/api/activities` | 是 | `ADMIN`/`PUBLISHER` | 后台活动列表 |
| 9 | `POST` | `/api/activities/{activityId}/publish` | 是 | `ADMIN`/`PUBLISHER` | 按配置发布活动 |
| 10 | `POST` | `/api/activities/{activityId}/advance-publish` | 是 | `ADMIN`/`PUBLISHER` | 提前发布活动 |
| 11 | `POST` | `/api/activities/{activityId}/offline` | 是 | `ADMIN`/`PUBLISHER` | 下线活动 |
| 12 | `DELETE` | `/api/activities/{activityId}` | 是 | `ADMIN`/`PUBLISHER` | 删除未发布或已下线活动 |
| 13 | `POST` | `/api/activities/{activityId}/codes/import` | 是 | `ADMIN`/`PUBLISHER` | 导入第三方兑换码 |
| 14 | `GET` | `/api/activities/{activityId}/codes/import-batches` | 是 | `ADMIN`/`PUBLISHER` | 查询导入批次列表 |
| 15 | `GET` | `/api/activities/{activityId}/codes/import-batches/{batchNo}` | 是 | `ADMIN`/`PUBLISHER` | 查询导入批次详情与失败明细 |

## 7. 接口详情

### 7.1 用户注册

- 方法：`POST`
- 路径：`/api/users/register`
- 鉴权：否

请求体：

| 字段 | 类型 | 必填 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `username` | string | 是 | 非空，最大 64 位 | 用户名 |
| `password` | string | 是 | 非空，长度 8 到 64 位 | 登录密码 |
| `nickname` | string | 否 | 最大 64 位 | 昵称 |
| `phone` | string | 否 | 最大 32 位 | 手机号 |

请求示例：

```json
{
  "username": "buyer_new",
  "password": "FlashSale@123",
  "nickname": "抢购用户",
  "phone": "13800000001"
}
```

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-USER-001",
  "data": {
    "id": 4,
    "username": "buyer_new",
    "role": "USER",
    "status": "ENABLED",
    "nickname": "抢购用户",
    "phone": "13800000001"
  }
}
```

业务说明：

- 注册成功后默认角色为 `USER`
- 注册成功后默认状态为 `ENABLED`
- 密码入库前会使用 BCrypt 加密

常见失败场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `400` | `INVALID_ARGUMENT` | `用户名已存在` |

### 7.2 用户登录

- 方法：`POST`
- 路径：`/api/users/login`
- 鉴权：否

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `username` | string | 是 | 用户名 |
| `password` | string | 是 | 密码 |

请求示例：

```json
{
  "username": "buyer",
  "password": "FlashSale@123"
}
```

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-USER-002",
  "data": {
    "accessToken": "<JWT>",
    "user": {
      "id": 3,
      "username": "buyer",
      "role": "USER",
      "status": "ENABLED",
      "nickname": "普通用户",
      "phone": "13800000003"
    }
  }
}
```

业务说明：

- 登录成功后返回 JWT 访问令牌
- JWT 默认有效期为 `2h`
- 当前令牌内包含：
  - 用户 ID
  - 用户名
  - 角色

常见失败场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `400` | `INVALID_ARGUMENT` | `用户名或密码错误` |
| `400` | `INVALID_ARGUMENT` | `当前用户已被禁用` |

### 7.3 获取当前登录用户

- 方法：`GET`
- 路径：`/api/users/me`
- 鉴权：是

请求头：

| Header | 必填 | 说明 |
| --- | --- | --- |
| `Authorization` | 是 | 格式：`Bearer <accessToken>` |
| `X-Request-Id` | 否 | 请求流水号 |

请求示例：

```http
GET /api/users/me HTTP/1.1
Authorization: Bearer <accessToken>
X-Request-Id: REQ-USER-003
```

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-USER-003",
  "data": {
    "id": 3,
    "username": "buyer",
    "role": "USER",
    "status": "ENABLED",
    "nickname": "普通用户",
    "phone": "13800000003"
  }
}
```

常见失败场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `401` | `UNAUTHORIZED` | `缺少有效的 Bearer Token` |
| `401` | `UNAUTHORIZED` | `无效或已过期的访问令牌` |
| `401` | `UNAUTHORIZED` | `未登录或登录状态已失效` |

### 7.4 按 ID 查询用户

- 方法：`GET`
- 路径：`/api/users/{userId}`
- 鉴权：是
- 角色要求：`ADMIN` 或 `PUBLISHER`

路径参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `userId` | number | 是 | 用户 ID |

请求示例：

```http
GET /api/users/3 HTTP/1.1
Authorization: Bearer <accessToken>
X-Request-Id: REQ-USER-004
```

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-USER-004",
  "data": {
    "id": 3,
    "username": "buyer",
    "role": "USER",
    "status": "ENABLED",
    "nickname": "普通用户",
    "phone": "13800000003"
  }
}
```

常见失败场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `401` | `UNAUTHORIZED` | `缺少有效的 Bearer Token` |
| `401` | `UNAUTHORIZED` | `无效或已过期的访问令牌` |
| `403` | `FORBIDDEN` | `当前用户无权访问该资源` |
| `400` | `INVALID_ARGUMENT` | `用户不存在` |

### 7.5 创建活动

- 方法：`POST`
- 路径：`/api/activities`
- 鉴权：是
- 角色要求：`ADMIN` 或 `PUBLISHER`

请求体示例：

```json
{
  "title": "5元礼品卡秒杀",
  "description": "限量兑换",
  "coverUrl": "https://example.com/activity.png",
  "totalStock": 100,
  "priceAmount": 0,
  "needPayment": false,
  "purchaseLimitType": "SINGLE",
  "purchaseLimitCount": 1,
  "codeSourceMode": "SYSTEM_GENERATED",
  "publishMode": "IMMEDIATE",
  "startTime": "2026-04-16 10:00:00",
  "endTime": "2026-04-16 10:30:00"
}
```

业务说明：

- `publishMode=IMMEDIATE` 时，创建成功后会立即发布，`publishStatus` 直接为 `PUBLISHED` 并同步完成缓存预热。
- `publishMode=SCHEDULED` 时必须传 `publishTime`，且 `publishTime` 不能晚于 `startTime`；创建后保持 `UNPUBLISHED`，等待到点或手动提前发布。
- `availableStock` 会自动初始化为 `totalStock`
- 用户侧公开活动可通过 `/api/public/activities` 与 `/api/public/activities/{activityId}` 查询

### 7.6 编辑活动

- 方法：`PUT`
- 路径：`/api/activities/{activityId}`
- 鉴权：是
- 角色要求：`ADMIN` 或 `PUBLISHER`

业务说明：

- 仅允许编辑 `UNPUBLISHED` 状态的活动
- 更新库存时会同步重置 `availableStock`
- `publishMode=IMMEDIATE` 时，编辑成功后会立即发布活动
- `publishMode=SCHEDULED` 时，仍需设置合法的 `publishTime`

### 7.7 查询活动详情

- 方法：`GET`
- 路径：`/api/activities/{activityId}`
- 鉴权：是
- 角色要求：`ADMIN` 或 `PUBLISHER`

业务说明：

- 返回活动基础字段以及派生状态 `phase`
- `phase` 当前取值：`PREVIEW`、`ONGOING`、`ENDED`

### 7.8 后台活动列表

- 方法：`GET`
- 路径：`/api/activities`
- 鉴权：是
- 角色要求：`ADMIN` 或 `PUBLISHER`

业务说明：

- 当前返回所有未删除活动
- 返回结构为 `ActivitySummaryResponse` 数组

### 7.9 发布活动

- 方法：`POST`
- 路径：`/api/activities/{activityId}/publish`
- 鉴权：是
- 角色要求：`ADMIN` 或 `PUBLISHER`

业务说明：

- `publishMode=IMMEDIATE` 时会立即切换为 `PUBLISHED`、刷新 `publishTime` 为当前时间，并预热 Redis 库存、活动详情和可见活动索引
- `publishMode=SCHEDULED` 且 `publishTime` 仍未到时，仅保留 `UNPUBLISHED` 状态，等待调度器到点发布
- 定时活动如需提前生效，请调用 `/api/activities/{activityId}/advance-publish`
- `codeSourceMode=THIRD_PARTY_IMPORTED` 时会校验 `redeem_code` 表中的可用码量是否不少于活动库存

### 7.9.1 提前发布活动

- 方法：`POST`
- 路径：`/api/activities/{activityId}/advance-publish`
- 鉴权：是
- 角色要求：`ADMIN` 或 `PUBLISHER`

业务说明：

- 主要用于 `publishMode=SCHEDULED` 活动在到达 `publishTime` 前的手动提前发布。
- 调用成功后活动会立即切换为 `PUBLISHED`，`publishTime` 更新为当前时间，并执行缓存预热。
- `codeSourceMode=THIRD_PARTY_IMPORTED` 时同样会执行可用兑换码数量校验。

### 7.10 下线活动

- 方法：`POST`
- 路径：`/api/activities/{activityId}/offline`
- 鉴权：是
- 角色要求：`ADMIN` 或 `PUBLISHER`

业务说明：

- 下线后活动状态切换为 `OFFLINE`
- 会同步删除 Redis 库存 Key、活动详情 Key，并移除可见活动索引

### 7.11 删除活动

- 方法：`DELETE`
- 路径：`/api/activities/{activityId}`
- 鉴权：是
- 角色要求：`ADMIN` 或 `PUBLISHER`

业务说明：

- 删除采用逻辑删除，实际会将 `activity_product.is_deleted` 标记为 `1`
- 仅允许删除 `UNPUBLISHED` 或 `OFFLINE` 状态活动
- 删除后活动不会再出现在后台列表和详情查询中
- 删除时会同步清理 Redis 库存 Key、活动详情 Key 和可见活动索引

常见失败场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `400` | `INVALID_ARGUMENT` | `仅未发布或已下线活动允许删除` |
| `400` | `INVALID_ARGUMENT` | `活动不存在` |

### 7.12 导入第三方兑换码

- 方法：`POST`
- 路径：`/api/activities/{activityId}/codes/import`
- 鉴权：是
- 角色要求：`ADMIN` 或 `PUBLISHER`
- `Content-Type`：`multipart/form-data`

路径参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `activityId` | number | 是 | 活动 ID |

表单参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `file` | file | 是 | 兑换码文件，仅支持 `csv`、`xlsx` |

请求示例：

```http
POST /api/activities/1001/codes/import HTTP/1.1
Authorization: Bearer <accessToken>
X-Request-Id: REQ-ACTIVITY-IMPORT-001
Content-Type: multipart/form-data
```

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-ACTIVITY-IMPORT-001",
  "data": {
    "batchNo": "IMP-1001-20260418153000001-1234",
    "fileName": "codes.csv",
    "totalCount": 5,
    "successCount": 2,
    "failedCount": 3,
    "failures": [
      {
        "lineNumber": 3,
        "rawCode": "CODE1001",
        "reason": "DUPLICATE_IN_FILE"
      },
      {
        "lineNumber": 4,
        "rawCode": "BAD CODE",
        "reason": "INVALID_FORMAT"
      },
      {
        "lineNumber": 5,
        "rawCode": "",
        "reason": "EMPTY_CODE"
      }
    ]
  }
}
```

业务说明：

- 仅允许 `UNPUBLISHED` 且 `codeSourceMode=THIRD_PARTY_IMPORTED` 的活动导入兑换码
- 支持识别首行表头：`code`、`redeem_code`、`兑换码`
- 当前 CSV/XLSX 都按首列读取兑换码
- 成功导入的兑换码写入 `redeem_code` 表，状态固定为 `AVAILABLE`
- 失败行会按批次写入失败明细表，当前失败原因包括：
  - `EMPTY_CODE`
  - `INVALID_FORMAT`
  - `DUPLICATE_IN_FILE`
  - `DUPLICATE_IN_SYSTEM`

常见失败场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `400` | `INVALID_ARGUMENT` | `仅未发布活动允许导入兑换码` |
| `400` | `INVALID_ARGUMENT` | `仅第三方导入模式活动允许导入兑换码` |
| `400` | `INVALID_ARGUMENT` | `导入文件不能为空` |
| `400` | `INVALID_ARGUMENT` | `导入文件中没有可解析的数据` |
| `400` | `INVALID_ARGUMENT` | `仅支持 csv 或 xlsx 文件导入` |
| `400` | `INVALID_ARGUMENT` | `导入文件解析失败` |

### 7.13 查询导入批次列表

- 方法：`GET`
- 路径：`/api/activities/{activityId}/codes/import-batches`
- 鉴权：是
- 角色要求：`ADMIN` 或 `PUBLISHER`

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-ACTIVITY-IMPORT-002",
  "data": [
    {
      "batchNo": "IMP-1001-20260418153000001-1234",
      "fileName": "codes.csv",
      "totalCount": 100,
      "successCount": 96,
      "failedCount": 4
    }
  ]
}
```

业务说明：

- 返回指定活动下所有未删除导入批次
- 当前按批次创建顺序倒序返回，最新批次在前

### 7.14 查询导入批次详情

- 方法：`GET`
- 路径：`/api/activities/{activityId}/codes/import-batches/{batchNo}`
- 鉴权：是
- 角色要求：`ADMIN` 或 `PUBLISHER`

路径参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `activityId` | number | 是 | 活动 ID |
| `batchNo` | string | 是 | 导入批次号 |

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-ACTIVITY-IMPORT-003",
  "data": {
    "batchNo": "IMP-1001-20260418153000001-1234",
    "fileName": "codes.csv",
    "totalCount": 5,
    "successCount": 2,
    "failedCount": 3,
    "failures": [
      {
        "lineNumber": 4,
        "rawCode": "BAD CODE",
        "reason": "INVALID_FORMAT"
      }
    ]
  }
}
```

常见失败场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `400` | `INVALID_ARGUMENT` | `导入批次不存在` |
| `400` | `INVALID_ARGUMENT` | `活动不存在` |

### 7.15 用户侧公开活动列表

- 方法：`GET`
- 路径：`/api/public/activities`
- 鉴权：否

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-PUBLIC-ACTIVITY-001",
  "data": [
    {
      "id": 1002,
      "title": "五一电影兑换活动",
      "totalStock": 200,
      "availableStock": 160,
      "publishMode": "IMMEDIATE",
      "publishStatus": "PUBLISHED",
      "phase": "PREVIEW",
      "publishTime": "2026-04-19T09:00:00",
      "startTime": "2026-04-20T10:00:00",
      "endTime": "2026-04-21T23:59:59"
    }
  ]
}
```

业务说明：

- 仅返回 `PUBLISHED` 且未删除的活动
- 当前按 `startTime` 倒序、`id` 倒序返回，开始时间越晚的活动越靠前
- 用户侧可见活动可能处于 `PREVIEW`、`ONGOING`、`ENDED` 三种阶段，`phase` 由发布时间窗口计算得出
- `UNPUBLISHED`、`OFFLINE` 或逻辑删除活动不会出现在该列表中

### 7.16 用户侧公开活动详情

- 方法：`GET`
- 路径：`/api/public/activities/{activityId}`
- 鉴权：否

路径参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `activityId` | number | 是 | 活动 ID |

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-PUBLIC-ACTIVITY-002",
  "data": {
    "id": 1002,
    "title": "五一电影兑换活动",
    "description": "面向用户公开展示的兑换活动详情示例",
    "coverUrl": "https://example.com/activity/1002.png",
    "totalStock": 200,
    "availableStock": 160,
    "priceAmount": 0,
    "needPayment": false,
    "purchaseLimitType": "SINGLE",
    "purchaseLimitCount": 1,
    "codeSourceMode": "SYSTEM_GENERATED",
    "publishMode": "IMMEDIATE",
    "publishStatus": "PUBLISHED",
    "phase": "PREVIEW",
    "publishTime": "2026-04-19T09:00:00",
    "startTime": "2026-04-20T10:00:00",
    "endTime": "2026-04-21T23:59:59"
  }
}
```

业务说明：

- 仅允许查询 `PUBLISHED` 且未删除的活动详情
- 返回结构与后台活动详情接口保持一致，便于前端复用 `ActivityDetailResponse`
- 公开详情接口不会暴露未发布、已下线或已删除活动，用户侧统一视为不存在

常见失败场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `400` | `INVALID_ARGUMENT` | `活动不存在` |

### 7.17 秒杀资格尝试

- 方法：`POST`
- 路径：`/api/seckill/activities/{activityId}/attempt`
- 鉴权：是

请求头：

| Header | 必填 | 说明 |
| --- | --- | --- |
| `Authorization` | 是 | `Bearer <token>` |
| `X-Request-Id` | 是 | 请求幂等标记 |

路径参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `activityId` | number | 是 | 活动 ID |

成功响应示例：

```json
{
  "code": "SECKILL_PROCESSING",
  "message": "请求已受理，请稍后查询结果",
  "requestId": "REQ-SECKILL-001",
  "data": {
    "activityId": 1002,
    "status": "PROCESSING"
  }
}
```

业务说明：

- 秒杀入口要求用户已登录，并由 Gateway 透传 `X-User-Id`、`X-Username`、`X-Role`
- 服务会先读取活动缓存并校验活动是否仍处于 `PUBLISHED`
- Lua 脚本在一次执行中完成时间窗校验、重复请求校验、限购校验和库存预扣
- Lua 校验通过后发送 `order.create` 消息，并写入 `seckill:result:{activityId}:{userId}` 为 `PROCESSING`
- 免费活动会在订单服务消费 `order.create` 后直接发码，并将结果回写为 `SUCCESS/FAIL`
- 支付活动会先创建 `WAIT_PAY` 订单并把结果回写为 `PENDING_PAYMENT`
- 支付回调成功后，订单服务会消费 `payment.success` 更新订单并发码
- 待支付订单超时关闭后，订单服务会消费 `order.timeout.close` 完成库存与限购回补，并把结果回写为 `FAIL`

常见返回场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `200` | `SECKILL_PROCESSING` | `请求已受理，请稍后查询结果` |
| `200` | `ACTIVITY_NOT_STARTED` | `活动未开始` |
| `200` | `ACTIVITY_ENDED` | `活动已结束` |
| `200` | `ACTIVITY_OFFLINE` | `活动已下线或未发布` |
| `200` | `OUT_OF_STOCK` | `库存不足` |
| `200` | `DUPLICATE_REQUEST` | `重复请求` |
| `200` | `OVER_PURCHASE_LIMIT` | `超出限购` |
| `400` | `INVALID_ARGUMENT` | `缺少 X-Request-Id 请求头` |

### 7.18 秒杀结果查询

- 方法：`GET`
- 路径：`/api/seckill/results/{activityId}`
- 鉴权：是

路径参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `activityId` | number | 是 | 活动 ID |

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-SECKILL-002",
  "data": {
    "status": "SUCCESS",
    "orderNo": "SO202604190001",
    "message": "抢购成功",
    "code": "ABCDEF123456",
    "updatedAt": "2026-04-19T10:05:00"
  }
}
```

无结果时响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-SECKILL-003",
  "data": {
    "status": "INIT",
    "orderNo": null,
    "message": "暂无抢购结果",
    "code": null,
    "updatedAt": null
  }
}
```

业务说明：

- 当前接口从 `seckill:result:{activityId}:{userId}` 读取用户自己的秒杀结果缓存
- 当用户尚未发起秒杀，或结果缓存尚未建立时，返回 `INIT`
- 免费活动会回写 `SUCCESS` 或 `FAIL`
- 支付活动在支付前会回写 `PENDING_PAYMENT`
- 支付成功并发码后会更新为 `SUCCESS`
- 支付超时关闭后会更新为 `FAIL`，`message` 为 `支付超时，订单已关闭`

### 7.19 创建模拟支付单

- 方法：`POST`
- 路径：`/api/payments/orders/{orderNo}`
- 鉴权：是

请求头：

| Header | 必填 | 说明 |
| --- | --- | --- |
| `Authorization` | 是 | `Bearer <token>` |
| `X-Request-Id` | 否 | 用于链路追踪，响应原样回传 |

路径参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `orderNo` | string | 是 | 待支付订单号 |

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-PAYMENT-301",
  "data": {
    "orderNo": "SO202604190301",
    "transactionNo": "TXN202604192048000001",
    "payAmount": 59.90,
    "payStatus": "INIT"
  }
}
```

业务说明：

- 仅允许已登录用户为**自己的** `WAIT_PAY` 订单发起模拟支付
- 首次调用会创建一条 `payment_record` 并返回 `transactionNo`
- 同一订单重复创建时会复用已存在的支付记录，不会重复落库
- 创建支付单后会发送延时关单消息，超时仍未支付的订单会进入关闭补偿流程

常见返回场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `200` | `SUCCESS` | `ok` |
| `400` | `INVALID_ARGUMENT` | `订单不存在` |
| `400` | `INVALID_ARGUMENT` | `该订单无需支付` |
| `400` | `INVALID_ARGUMENT` | `该订单已支付` |
| `400` | `INVALID_ARGUMENT` | `该订单已关闭` |
| `401` | `UNAUTHORIZED` | `未登录或登录状态已失效` |
| `403` | `FORBIDDEN` | `无权为该订单发起支付` |

### 7.20 模拟支付回调

- 方法：`POST`
- 路径：`/api/payments/callback`
- 鉴权：否

请求体：

```json
{
  "orderNo": "SO202604190301",
  "transactionNo": "TXN202604192048000001"
}
```

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-PAYMENT-302",
  "data": {
    "orderNo": "SO202604190301",
    "transactionNo": "TXN202604192048000001",
    "payAmount": 59.90,
    "payStatus": "SUCCESS"
  }
}
```

业务说明：

- 回调按 `transactionNo` 做幂等，重复回调不会重复发送 `payment.success`
- 支付服务只更新支付记录并发送支付成功事件，不直接发码
- 若对应订单已关闭，则回调会被拒绝

常见返回场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `200` | `SUCCESS` | `ok` |
| `400` | `INVALID_ARGUMENT` | `订单不存在` |
| `400` | `INVALID_ARGUMENT` | `支付流水不存在` |
| `400` | `INVALID_ARGUMENT` | `支付流水与订单不匹配` |
| `400` | `INVALID_ARGUMENT` | `订单已关闭，不能再支付` |

### 7.21 按订单号查询订单详情

- 方法：`GET`
- 路径：`/api/orders/{orderNo}`
- 鉴权：是

请求头：

| Header | 必填 | 说明 |
| --- | --- | --- |
| `Authorization` | 是 | `Bearer <token>` |
| `X-Request-Id` | 否 | 用于链路追踪，响应原样回传 |

路径参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `orderNo` | string | 是 | 订单号 |

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-ORDER-001",
  "data": {
    "orderNo": "SO202604200001",
    "activityId": 1002,
    "userId": 3001,
    "orderStatus": "CONFIRMED",
    "payStatus": "PAID",
    "codeStatus": "ISSUED",
    "priceAmount": 29.90,
    "updatedAt": "2026-04-20T10:05:00"
  }
}
```

业务说明：

- 仅允许已登录用户查询**自己的**订单详情
- 免费活动成功发码后，通常为 `orderStatus=CONFIRMED`、`payStatus=NO_NEED`、`codeStatus=ISSUED`
- 支付活动待支付阶段会返回 `orderStatus=INIT`、`payStatus=WAIT_PAY`、`codeStatus=PENDING`
- 若订单因支付超时或异常关闭，`failReason` 会返回失败原因

常见返回场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `200` | `SUCCESS` | `ok` |
| `400` | `INVALID_ARGUMENT` | `订单不存在` |
| `401` | `UNAUTHORIZED` | `未登录或登录状态已失效` |
| `403` | `FORBIDDEN` | `无权查看该订单` |

### 7.22 按订单号查询兑换码

- 方法：`GET`
- 路径：`/api/codes/orders/{orderNo}`
- 鉴权：是

请求头：

| Header | 必填 | 说明 |
| --- | --- | --- |
| `Authorization` | 是 | `Bearer <token>` |
| `X-Request-Id` | 否 | 用于链路追踪，响应原样回传 |

路径参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `orderNo` | string | 是 | 订单号 |

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-CODE-001",
  "data": {
    "orderNo": "SO202604190101",
    "activityId": 1002,
    "orderStatus": "CONFIRMED",
    "payStatus": "NO_NEED",
    "codeStatus": "ISSUED",
    "code": "QUERY-CODE-001",
    "updatedAt": "2026-04-19T10:05:00"
  }
}
```

业务说明：

- 仅允许已登录用户查询**自己的**订单兑换码
- 免费活动成功发码后，`payStatus` 为 `NO_NEED`
- 支付活动支付成功并发码后，`payStatus` 为 `PAID`
- 当订单存在但尚未发码，例如支付型活动仍处于待支付阶段时，`code` 字段会返回 `null`

常见返回场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `200` | `SUCCESS` | `ok` |
| `400` | `INVALID_ARGUMENT` | `订单不存在` |
| `401` | `UNAUTHORIZED` | `未登录或登录状态已失效` |
| `403` | `FORBIDDEN` | `无权查看该订单` |

### 7.23 创建导出任务

- 方法：`POST`
- 路径：`/api/exports/tasks`
- 鉴权：是，要求 `ADMIN` 或 `PUBLISHER`

请求头：

| Header | 必填 | 说明 |
| --- | --- | --- |
| `Authorization` | 是 | `Bearer <token>` |
| `X-Request-Id` | 否 | 用于链路追踪，响应原样回传 |

请求体：

```json
{
  "activityId": 1002,
  "format": "CSV",
  "filters": {
    "payStatus": "PAID",
    "codeStatus": "ISSUED"
  }
}
```

说明：

- `format` 仅支持 `CSV`、`XLSX`
- `filters` 当前支持按 `payStatus`、`orderStatus`、`codeStatus`、`userId` 过滤

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-EXPORT-001",
  "data": {
    "id": 8,
    "activityId": 1002,
    "operatorId": 1,
    "format": "CSV",
    "filters": {
      "payStatus": "PAID",
      "codeStatus": "ISSUED"
    },
    "status": "INIT",
    "createdAt": "2026-04-20T09:30:00",
    "updatedAt": "2026-04-20T09:30:00"
  }
}
```

业务说明：

- 创建成功后会先写入 `export_task`，然后发送 `export.generate` 异步事件
- 任务初始状态为 `INIT`，消费者处理中会转为 `PROCESSING`
- 导出成功后会回填 `fileUrl`，失败则回填 `failReason`

常见返回场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `200` | `SUCCESS` | `ok` |
| `400` | `INVALID_ARGUMENT` | `活动不存在` |
| `400` | `INVALID_ARGUMENT` | `导出格式不能为空` |
| `400` | `INVALID_ARGUMENT` | `仅支持 CSV 或 XLSX 导出` |
| `401` | `UNAUTHORIZED` | `未登录或登录状态已失效` |
| `403` | `FORBIDDEN` | `当前用户无权访问该资源` |

### 7.24 查询导出任务详情

- 方法：`GET`
- 路径：`/api/exports/tasks/{taskId}`
- 鉴权：是，要求 `ADMIN` 或 `PUBLISHER`

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-EXPORT-002",
  "data": {
    "id": 8,
    "activityId": 1002,
    "operatorId": 1,
    "format": "CSV",
    "filters": {
      "payStatus": "PAID"
    },
    "status": "SUCCESS",
    "fileUrl": "/api/exports/files/activity-1002-export-8-20260420093000.csv",
    "createdAt": "2026-04-20T09:30:00",
    "updatedAt": "2026-04-20T09:30:05"
  }
}
```

常见返回场景：

| HTTP 状态码 | 业务码 | message |
| --- | --- | --- |
| `200` | `SUCCESS` | `ok` |
| `400` | `INVALID_ARGUMENT` | `导出任务不存在` |
| `401` | `UNAUTHORIZED` | `未登录或登录状态已失效` |
| `403` | `FORBIDDEN` | `当前用户无权访问该资源` |

### 7.25 按活动查询导出任务列表

- 方法：`GET`
- 路径：`/api/exports/tasks?activityId={activityId}`
- 鉴权：是，要求 `ADMIN` 或 `PUBLISHER`

业务说明：

- 按 `activityId` 倒序返回该活动下的导出任务
- 可用于轮询导出状态和下载地址

### 7.26 查询异常补偿台账

- 方法：`GET`
- 路径：`/api/exports/compensations`
- 鉴权：是，要求 `ADMIN` 或 `PUBLISHER`

成功响应示例：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "requestId": "REQ-EXPORT-003",
  "data": [
    {
      "id": 3,
      "bizType": "EXPORT_TASK",
      "bizKey": "export-task:8",
      "sourceEvent": "export.generate.dead",
      "status": "PENDING",
      "reason": "导出文件生成失败",
      "createdAt": "2026-04-20T09:32:00"
    }
  ]
}
```

业务说明：

- 当前台账主要记录导出链路死信与异常补偿项
- 后续扩展订单、支付等消费补偿时，可继续复用该接口

### 7.27 处理异常补偿记录

- 方法：`POST`
- 路径：`/api/exports/compensations/{compensationId}/resolve`
- 鉴权：是，要求 `ADMIN` 或 `PUBLISHER`

请求体：

```json
{
  "resolutionNote": "已人工补发导出文件并通知运营下载"
}
```

业务说明：

- 处理成功后会将台账状态更新为 `RESOLVED`
- 会同步写入一条 `COMPENSATION_RESOLVED` 审计日志

### 7.28 下载导出文件

- 方法：`GET`
- 路径：`/api/exports/files/{fileName}`
- 鉴权：是，要求 `ADMIN` 或 `PUBLISHER`

业务说明：

- 仅允许下载订单服务导出目录内的已生成文件
- 文件路径由导出任务详情中的 `fileUrl` 返回
- 响应为附件下载流，格式与任务 `format` 保持一致

## 8. 默认联调账号

数据库首次初始化后，会写入以下默认用户，默认密码统一为 `FlashSale@123`：

| 用户名 | 角色 | 用途 |
| --- | --- | --- |
| `admin` | `ADMIN` | 后台管理与角色校验联调 |
| `publisher` | `PUBLISHER` | 发布方/后台接口联调 |
| `buyer` | `USER` | 普通用户联调 |

## 9. 当前未实现接口说明

当前文档已覆盖仓库里已经落地并可直接联调的核心接口。

尚未补齐的主要是**更完整的扩展能力**，例如后台订单列表、更多支付异常场景、真实第三方支付接入等；联调时请以本文列出的接口为准。
