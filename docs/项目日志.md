# 项目日志

## 2026-04-14

### 事项

- 开始实施《高并发秒杀兑换平台详细设计与实施拆解文档》中的 `Task 1: 基础工程与基础设施`。

### 已完成

- 创建根聚合工程 `pom.xml`，统一 Java、Spring Boot、Spring Cloud 和 Spring Cloud Alibaba 版本。
- 创建 `flash-sale-common` 聚合模块及 `common-core`、`common-security`、`common-redis`、`common-mq`、`common-web` 5 个公共模块。
- 创建 `flash-sale-gateway`、`flash-sale-user-service`、`flash-sale-activity-service`、`flash-sale-seckill-service`、`flash-sale-order-service`、`flash-sale-payment-service` 6 个业务模块。
- 先写公共模块测试，再补最小实现，完成 `ApiResponse`、`UserContext`、`RedisKeys`、`DomainEvent` 等基础类。
- 新增 `deploy/docker-compose.yml`，覆盖 MySQL、Redis、RabbitMQ、Nacos 本地联调环境。
- 新增 `deploy/sql/01_schema.sql`，初始化核心表：`user`、`activity_product`、`redeem_code`、`order_record`、`payment_record`、`export_task`。
- 新增 `.env.example` 与 `README.md`，明确端口、环境变量和本地启动方式。
- 新增 `docs/superpowers/plans/2026-04-14-task1-m0-scaffold.md`，沉淀 Task 1 执行计划。

### 验证

- 公共模块测试采用 TDD 完成红绿循环。
- 下一步将执行聚合构建与 Docker Compose 配置校验，确认 Task 1 交付物可被本地工具链识别。

### 风险与待办

- 当前业务服务仅完成启动骨架和配置占位，尚未进入认证、网关鉴权和业务接口实现。
- Nacos 默认通过环境变量关闭服务注册，后续进入联调阶段需要根据实际环境开启。
- 后续 Task 2 需要补充种子用户 SQL、JWT 组件和网关透传逻辑。

### 事项

- 开始实施《高并发秒杀兑换平台详细设计与实施拆解文档》中的 `Task 2: 认证与权限`。

### 已完成

- 新增 `docs/superpowers/plans/2026-04-14-task2-auth-and-rbac.md`，沉淀 Task 2 的 TDD 执行计划。
- 将 MyBatis Plus 切换到 Spring Boot 3 对应 starter，修复 `mapper` 扫描兼容问题。
- 在 `common-security` 中新增 JWT、用户上下文持有器、角色校验注解与切面、Servlet 过滤器和自动装配配置。
- 在 `common-web` 中补齐认证鉴权异常到统一响应体的映射，并改为自动装配。
- 在 `flash-sale-user-service` 中实现注册、登录、当前用户查询、按 ID 查询接口，以及对应实体、Mapper、服务和 DTO。
- 在 `flash-sale-gateway` 中新增 `JwtAuthenticationFilter`，支持公开接口放行、Bearer Token 校验和标准 Header 透传。
- 新增 `deploy/sql/02_seed_user.sql`，提供 `admin`、`publisher`、`buyer` 三个默认联调账号。
- 更新 `.env.example` 和 `README.md`，补充 JWT TTL 配置与默认联调账号说明。
- 修复 `common-security` 自动装配条件，避免未配置 JWT 密钥的服务在启动阶段错误创建 `JwtTokenService`。
- 对齐本地基础设施联调配置：MySQL 宿主机端口改为 `3307`，Redis Compose 根据 `REDIS_PASSWORD` 决定是否启用密码校验，MySQL 健康检查改为真实鉴权查询。
- 将 `flash-sale-gateway` 本地默认端口调整为 `18080`，避开部分 Windows 环境 `8080` 落入系统排除端口段导致的启动失败。
- 为各微服务增加对模块目录和仓库根目录 `.env` 的自动导入，避免单独启动模块时因未显式注入环境变量而出现 Redis、RabbitMQ 或 MySQL 认证失败。

### 验证

- 按 TDD 先写 `common-security`、`flash-sale-user-service`、`flash-sale-gateway` 测试，再补最小实现。
- 已执行 `mvn -q -pl flash-sale-common/common-security,flash-sale-user-service,flash-sale-gateway -am test` 并通过。
- 已在当前 `.env` 下逐个拉起 `flash-sale-user-service`、`flash-sale-activity-service`、`flash-sale-seckill-service`、`flash-sale-order-service`、`flash-sale-payment-service`、`flash-sale-gateway`，并验证 `/actuator/health` 返回 `UP`。
- 已验证 `flash-sale-activity-service` 在不手动注入环境变量、仅依赖自动导入根目录 `.env` 的情况下，`/actuator/health` 返回 `UP`，不再出现 Redis `NOAUTH` 导致的 `RedisConnectionFailureException`。

### 风险与待办

- 默认账号 SQL 仅在 MySQL 数据目录首次初始化时自动执行，已有本地卷需要手动重建或补执行脚本。
- 若本地已存在 MySQL/Redis 数据卷，再修改 `.env` 中的账号或密码，需要同步更新已有卷内状态，否则服务仍会因认证失败无法启动。
- 当前网关仅完成最小 JWT 鉴权链路，尚未加入 `X-Request-Id` 生成、请求日志和限流。
- 后续 Task 3 需要直接复用 `@RequireRole` 到活动后台接口。

## 2026-04-16

### 事项

- 开始实施《高并发秒杀兑换平台详细设计与实施拆解文档》中的 `Task 3: 活动管理与发布`。

### 已完成

- 新增 `docs/superpowers/plans/2026-04-16-task3-activity-management-and-publish.md`，沉淀 Task 3 执行计划。
- 为 `flash-sale-activity-service` 补齐 `spring-boot-starter-test`、`h2` 测试依赖和 H2 测试基线。
- 采用 TDD 先写 `ActivityAdminControllerTest`、`ActivityPublishSchedulerTest`、`ActivityCacheServiceTest`，覆盖活动 CRUD、立即发布、定时发布、下线、调度发布和 Redis 预热。
- 在活动服务中实现活动实体、枚举、Mapper、后台控制器、活动服务、发布调度器和 Redis 缓存预热组件。
- 复用 `@RequireRole({"ADMIN", "PUBLISHER"})` 为活动后台接口增加角色限制。
- 在 `common-redis` 中补充 `activity:visible:list` Key，发布时同步预热库存、活动详情和可见活动索引。
- 实现发布前基础校验，包括时间窗校验、支付金额规则校验，以及第三方兑换码模式下的可用码量校验。

### 验证

- 已执行 `mvn -q -pl flash-sale-common/common-redis,flash-sale-activity-service -am test` 并通过。
- 下一步执行 `mvn -q -pl flash-sale-activity-service -am package -DskipTests`，确认模块可独立打包。

### 风险与待办

- 当前活动列表仅实现后台管理视角，用户侧可见活动筛选接口仍待后续任务补充。
- 发布前第三方码量校验已经落地，但真实导入接口、批次明细和失败明细仍需在 Task 4 完成。
- 当前异常响应尚未细分参数校验字段级错误，仍沿用统一 `INVALID_ARGUMENT` 返回。

### 事项

- 补充活动删除能力，允许后台删除未发布或已下线活动。

### 已完成

- 新增 `docs/superpowers/specs/2026-04-16-activity-delete-design.md`，沉淀活动删除设计边界。
- 新增 `docs/superpowers/plans/2026-04-16-activity-delete.md`，拆分活动删除实现步骤。
- 在 `flash-sale-activity-service` 中新增 `DELETE /api/activities/{activityId}` 接口，采用逻辑删除并限制为 `UNPUBLISHED`、`OFFLINE` 状态可删。
- 删除时复用现有 `ActivityCacheService.clear` 清理 Redis 库存、详情和活动可见索引。
- 在后台活动列表页新增删除按钮、确认弹窗和禁用态规则，已发布活动不可删除。
- 新增后端删除接口测试和前端活动状态工具测试，覆盖删除规则与提示边界。

### 验证

- 已执行 `mvn -q -pl flash-sale-activity-service -am "-Dtest=ActivityAdminControllerTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` 并通过。
- 下一步执行活动服务全量测试、前端 `npm test` 与 `npm run build`，确认删除改动不会破坏现有活动管理流程。

### 风险与待办

- 当前删除为逻辑删除，数据库仍保留活动记录；如后续需要回收站或恢复功能，可在此基础上继续扩展。
- 本次删除未联动处理兑换码等关联数据，仅保证活动自身在后台查询与缓存层面消失。

## 2026-04-18

### 事项

- 开始实施《高并发秒杀兑换平台详细设计与实施拆解文档》中的 `Task 4: 第三方兑换码导入`。

### 已完成

- 为 `flash-sale-activity-service` 新增第三方兑换码导入接口：`POST /api/activities/{activityId}/codes/import`，支持 `csv/xlsx` 两种文件格式。
- 新增导入批次查询接口：`GET /api/activities/{activityId}/codes/import-batches` 与 `GET /api/activities/{activityId}/codes/import-batches/{batchNo}`。
- 在活动服务中新增 `redeem_code_import_batch`、`redeem_code_import_fail_detail` 两类导入追踪实体与查询能力，支持批次统计和失败行明细追溯。
- 实现导入文件解析、表头识别、空码校验、非法格式校验、文件内重复校验与系统内重复校验；成功码写入 `redeem_code`，失败项按行落明细表。
- 限制导入边界为“仅未发布且 `THIRD_PARTY_IMPORTED` 模式活动允许导入”，避免对已发布活动和系统生成码活动误操作。
- 更新 `deploy/sql/01_schema.sql` 与活动服务 H2 测试 `schema.sql`，补齐导入批次表和失败明细表。
- 采用 TDD 补充 `ActivityAdminControllerTest`，覆盖 CSV 导入、XLSX 导入、批次查询、失败明细查询和非法活动状态拦截。

### 验证

- 已执行 `mvn -q -pl flash-sale-activity-service -am "-Dtest=ActivityAdminControllerTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` 并通过。
- 已执行 `mvn -q -pl flash-sale-activity-service -am test` 并通过。

### 风险与待办

- 当前兑换码格式规则实现为 `6~128` 位的字母、数字、下划线和中划线；若后续存在更宽松的商家模板，需要在导入校验中放宽规则。
- CSV 导入当前按首列读取，适配“单列兑换码模板”；若后续要兼容多列模板或自定义列头，需要补充映射配置。
- 目前导入为同步处理，适合课程项目与本地联调；若后续单批导入量明显增大，可再演进为异步导入任务。
